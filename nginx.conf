server {
    listen 80;

    server_name localhost;

    location /media/ {
        alias /KinoCMS/media/;
        autoindex on;
    }

    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
}
